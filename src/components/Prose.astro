---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class={`prose md:prose-lg lg:prose-xl prose-figure:my-10 prose-headings:font-serif prose-headings:text-current text-current ${className}`}
>
  <slot />
</div>
